import { XmlNode } from './xmlNode';
export declare class XmlText extends XmlNode {
    /** The text content of the node. Special characters are expected to be escaped! */
    text: string;
    constructor(
    /** The text content of the node. Special characters are expected to be escaped! */
    text: string);
    unescapeText(): string;
    setText(unescapedText: string): void;
    isWhitespace(): boolean;
    toString(): string;
}
