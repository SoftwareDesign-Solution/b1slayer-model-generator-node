export declare class XmlAttribute {
    name: string;
    /** The attribute value. Special characters are expected to be escaped! */
    value: string;
    /** Whitespace immediately before the attribute name. */
    leadingWs: string;
    wsBeforeEqual: string;
    wsAfterEqual: string;
    quote: '"' | "'";
    hasValue: boolean;
    constructor(name: string, 
    /** The attribute value. Special characters are expected to be escaped! */
    value: string, 
    /** Whitespace immediately before the attribute name. */
    leadingWs?: string, wsBeforeEqual?: string, wsAfterEqual?: string, quote?: '"' | "'", hasValue?: boolean);
    unescapeValue(): string;
    setValue(unescapedValue: string): void;
    toString(): string;
}
