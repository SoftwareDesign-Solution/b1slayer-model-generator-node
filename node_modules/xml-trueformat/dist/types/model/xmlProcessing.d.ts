import { XmlNode } from './xmlNode';
export declare class XmlProcessing extends XmlNode {
    target: string;
    wsAfterTarget: string;
    data: string;
    constructor(target: string, wsAfterTarget: string, data: string);
    toString(): string;
}
